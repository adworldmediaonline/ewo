'use client';

// This component is no longer needed since Tawk.to is loaded via script in layout.jsx
// The script approach provides better performance and avoids hydration issues

export default function TawkToChat() {
  // Return null since Tawk.to is handled by the script in layout
  return null;
}
